import { Card, CardContent, CardHeader, CardTitle } from "@sgscore/ui";

export default function EmbedTicketsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tickets</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Embeddable ticket widget will be built here.
        </p>
      </CardContent>
    </Card>
  );
}
