import { Card, CardContent, CardHeader, CardTitle } from "@sgscore/ui";

export default function EmbedCheckoutPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Checkout</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Embeddable checkout widget will be built here.
        </p>
      </CardContent>
    </Card>
  );
}
