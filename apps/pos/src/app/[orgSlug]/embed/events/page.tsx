import { Card, CardContent, CardHeader, CardTitle } from "@sgscore/ui";

export default function EmbedEventsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Events</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Embeddable events widget will be built here.
        </p>
      </CardContent>
    </Card>
  );
}
