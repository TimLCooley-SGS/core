import { Card, CardContent, CardHeader, CardTitle } from "@sgscore/ui";

export default function EmbedMembershipsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Memberships</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Embeddable memberships widget will be built here.
        </p>
      </CardContent>
    </Card>
  );
}
